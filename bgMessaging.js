// @flow
import firebase from 'react-native-firebase';
// Optional flow type
import type { RemoteMessage } from 'react-native-firebase';

export default // handle your message
async (message: RemoteMessage) => Promise.resolve();
